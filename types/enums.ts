export enum pet_status {
	WAITING_APPROVAL,
	APPROVED,
	DELETED,
	DENIED,
	FOUND
}

export enum pet_report_steps {
	INFOS,
	PLACE,
	EXTRA_INFOS,
	REVIEW
}

export enum contact_method {
	PHONE,
	MAIL,
	INSTAGRAM
}
